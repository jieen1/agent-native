# CI probe (SDLC-096)

Temporary, harmless PR used to read the current real CI check matrix against
`main` while investigating SDLC-096 (`workspaceMergePr` fail-closed on stale
CI noise). Safe to close/delete without any follow-up — this file carries no
runtime behavior.
