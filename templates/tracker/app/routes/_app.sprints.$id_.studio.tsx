import { SprintStudioPage } from "@/pages/SprintStudioPage";

export function meta() {
  return [{ title: "Sprint 规划工作台 · Tracker" }];
}

export default function SprintStudioRoute() {
  return <SprintStudioPage />;
}
